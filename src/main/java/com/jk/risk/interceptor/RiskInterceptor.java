package com.jk.risk.interceptor;import com.jk.risk.util.Constants;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import org.springframework.web.servlet.handler.HandlerInterceptorAdapter;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;/** * 拦截器 * User: allen * Date: 2017/11/16 上午9:37 */public class RiskInterceptor extends HandlerInterceptorAdapter {    private final static Logger logger = LoggerFactory.getLogger(RiskInterceptor.class);    @Override    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {//        String token = request.getHeader(Constants.RISK_HEADER);//        if (StringUtils.isEmpty(token) || !token.equals("1qaz2wsx")) {//            logger.warn、("Found error token, This is a unauthorized request.");//            return false;//        }        request.setAttribute(Constants.REQUEST_TIME, System.currentTimeMillis());        return true;    }}